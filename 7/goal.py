# Import the modules you need
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import scipy.stats as stats

# Define your goal as a string
goal = "Increase the click-through rate (CTR) of the ads"

# Define your hypothesis as a string
hypothesis = "Changing the color of the call-to-action (CTA) button will affect the CTR"

# Print your goal and hypothesis
print(f"Goal: {goal}")
print(f"Hypothesis: {hypothesis}")
